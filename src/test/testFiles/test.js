console.log("HelloWorld");
function test() {}
class Test {}
let arr = [1, 2, 3, 4, 5];
let test = arr
  .map((a) => a + 1)
  .filter((a) => a > 3)
  .reduce((a, b) => a + b);
new RegExp(aaaaaaaaaaaa).split();
// comment
let a = 1 ? arr : 2;
switch (a) {
  default:
    a = 1;
    break;
}
a = a << 1;
a = Math.random();
a = () => {
  return 1;
};
try {
  a = 1;
} catch (e) {
  a = 2;
}
// now js only
new Map();
b = [...arr];
/*
 * @param {string} a
 */
async function test() {
  new Promise();
}
require("fs");
a = [...new Set(arr)];
a = structuredClone(arr);
